# Copy this file to .env in the repo root and fill in real values.
# Never commit .env; it is in .gitignore.

# Optional. Get one at https://platform.deepseek.com/. Without it, the deterministic fallback renders the same site shape from project metadata.
DEEPSEEK_API_KEY=sk-placeholder

# Optional override; deepseek-chat resolves to V4 latest.
DEEPSEEK_MODEL_FAST=deepseek-chat
